name: app_shimmer
description: A customizable Flutter shimmer loading package with pre-built widgets for ListTiles, ListViews, Grids, and custom containers. Easy to use and highly customizable.
version: 1.0.2
homepage: https://github.com/cristiancamolo070/app_shimmer
repository: https://github.com/cristiancamolo070/app_shimmer
issue_tracker: https://github.com/cristiancamolo070/app_shimmer/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_skeleton_ui: ^0.0.6

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

topics:
  - shimmer
  - loading
  - skeleton
  - ui
  - widgets

flutter:
